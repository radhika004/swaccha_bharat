
// Firebase configuration has been removed as the application is now frontend-only.

// Placeholder exports to avoid breaking imports immediately.
// These should be removed or replaced with mock implementations later.
export const app = null;
export const auth = null;
export const db = null;
export const storage = null;
export const analytics = null;

/*
============================================
Firebase Integration Removed
============================================

The Firebase backend integration (Authentication, Firestore, Storage)
has been removed from this application as per the request to make it
frontend-only.

The remaining code uses mock data and frontend logic.
============================================
*/
